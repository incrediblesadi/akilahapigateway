# REPO MAP_DEV\n
## Context Files: src/session/

```
- buildCurrentSession.js : Generates a cashed session object using user data and Firebase
- currentSession.json: Stores active session information in simple JSON
- index.js: Initializes the session module with context users can call]r
- rebuild.js: Resets session data to default state for testing/repository switches
```
